#include <stdio.h>
#define N 8
//输入数组，最大的与第一个元素交换，最小的与最后一个元素交换，输出数组。
int main()
{
	int arr[N]={3,2,5,1,9,6,4,7};
	int i,tmp,max=0,min;
	for(i=0;i<N;i++)
	{
		if(arr[i]>max)
		{
			max=arr[i];
		}
	}
	printf("max=%d",max);	
	return 0;
}
